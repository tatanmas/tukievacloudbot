# 2026-03-30

- Integración **Tuki API** (solo lectura): skill `skills/tuki-integration-api/`, script `scripts/tuki_api.sh`, variable `TUKI_INTEGRATION_TOKEN` pasada en `docker-compose.yml`. Flujo: capabilities → snapshot u orders según pregunta y scopes del token.
- API integración en prod: tras fix DRF (sin JWT sobre Bearer en esas vistas), **capabilities / snapshot / orders** responden **200** con token `tuki_...`. Script: `skills/tuki-integration-api/scripts/tuki_api.sh`.
- Reorden de repo: `data/leads/`, `archive/contexto-migrado/`, `content/tuki/`, `vendor/google-maps-scraper/`, `docs/notes/`; raíz conserva contrato Clawdbot. Ver `docs/STRUCTURE.md`.
- Growth: `SOUL.md` + `HEARTBEAT.md` + `docs/GROWTH-EVA.md`; integración Tuki probada en contenedor tras `docker compose up -d --force-recreate`.
- Docs red agentes + sync Notion + prompt Cursor: `docs/RED-AGENTES-TUKI-NOTION.md`, `docs/CURSOR-PROMPT-EVA-SYNC-CARGA.md`, `docs/CHECKLIST-CARGA-TUKI.md` (plantilla).
- Pulso growth automático: `growth_pulse.sh` + `growth_pulse_summary.py` → `data/pulses/`; guía `docs/PULSO-GROWTH-CRON.md` (chat vs cron).
- Implementación prompt sync/carga: skill `skills/tuki-notion-sync/` (`sync_leads_from_export.py`, props `ID Tuki` / `Slug Tuki` / etc. en Notion), `tuki_pulse.sh` + `tuki_resource_lists_stub.sh` bajo `tuki-integration-api/scripts/`, `data/sync/` para CSV/JSON.
